# 新手场景：基于 5 个修炼主题，每天自动生成训练任务清单（OpenClaw）

## 结论先说

- 不需要 cookies。你已经提供了主题摘要，足够直接落地。
- OpenClaw 当前可用：`openclaw gateway health --json` 返回 `"ok": true`。
- 目标是让系统在每个工作日自动输出一份「当日训练任务清单」。

## 主题库（固定输入）

以下 5 个主题作为长期训练库，按工作日轮换：

1. 业务直觉与逻辑边界（Limit-Up 防御）
2. 系统依赖与蝴蝶效应推演（Architecture Mindset）
3. 用契约与规则替代人肉管理（Design is Management）
4. 团队杠杆与异步决策（1+3+N 阵型运作）
5. 职场算法与逆向工程（人际防御机制）

建议轮换顺序（你给出的节奏）：

- 周一：团队杠杆
- 周二：规则管理
- 周三：系统推演
- 周四：业务审判
- 周五：职场算法

## 一次性创建定时任务（可直接复制）

下面命令会创建一个「周一到周五 07:30（上海时区）」自动运行的任务。

```bash
PROMPT=$(cat <<'EOT'
你是“资深量化/业务开发成长教练”。请生成“今天的训练任务清单”。

用户画像：
- 8年资深量化/业务开发
- 目标：提升业务判断力、系统思维、团队杠杆和职场防御能力

周内主题映射（必须按当天星期自动选择）：
- 周一 -> 团队杠杆与异步决策
- 周二 -> 用契约与规则替代人肉管理
- 周三 -> 系统依赖与蝴蝶效应推演
- 周四 -> 业务直觉与逻辑边界
- 周五 -> 职场算法与逆向工程

5个主题细化动作库（当天仅选对应主题，但要从库中抽取可执行动作）：

主题一：业务直觉与逻辑边界
- 翻译缝隙审查：把因子自然语言转 if/else 伪代码，找模糊边界
- 极端物理场景代入：涨停/跌停/停牌/一字板/新股首日是否符合常识
- 赌工资终审：不敢真金白银跟信号就退回重定义边界

主题二：系统依赖与蝴蝶效应推演
- 画影响域矩阵：上游/下游/缓存清洗影响
- 生命周期状态机推演：草稿->测试->上线->废弃，重点边缘状态
- 异常熔断设计：超时10秒或空列表时系统是否降级与告警

主题三：用契约与规则替代人肉管理
- Schema绝对独裁：完善输入输出规范
- 防伪表格：需求改成带边界与物理含义的填空题
- 代码质检关卡：自动化脚本拦截不合格交付

主题四：团队杠杆与异步决策
- 忍住不敲键盘：让老员工给两套方案对比
- 拦截越级提问：新人先走mentor链路
- 批注RFC文档：只提漏洞，不直接给答案

主题五：职场算法与逆向工程
- XY问题剥离术：追问真实目标Y而非表象X
- 强制生成审计日志：口头结论转文字确认
- 睡前5分钟异常复盘：踩坑实录/人际预警

输出格式（严格遵守）：
1) 今日主题（1行）
2) 今日目标（2-3行，可量化）
3) 25分钟深度训练任务（3项，每项含：动作、产出物、完成标准）
4) 10分钟风险防御任务（2项，每项含：触发条件、应对动作）
5) 今日沟通话术（2段，可直接复用）
6) 晚间复盘模板（5个问题）
7) 明日衔接点（1-2行）

质量要求：
- 所有任务都必须可在“今天”完成，不能抽象
- 每个任务都要有可检查的产出（文档、脚本、评论、消息记录）
- 全中文输出，简洁但具体
EOT
)

openclaw cron add \
  --name "daily-quant-routine" \
  --description "工作日每日训练任务清单" \
  --cron "30 7 * * 1-5" \
  --tz "Asia/Shanghai" \
  --session isolated \
  --no-deliver \
  --timeout-seconds 180 \
  --thinking low \
  --message "$PROMPT"
```

## 第一次落地（不要等明天）

1. 查看任务是否创建成功

```bash
openclaw cron list --all --json
```

2. 立即手动触发一次

```bash
openclaw cron run <job_id>
```

3. 查看运行历史

```bash
openclaw cron runs --id <job_id> --limit 20
```

4. 查看输出内容

```bash
openclaw dashboard
```

## 新手第一周执行法（建议）

- 周一：只做“团队杠杆”，当天禁止亲自改代码解决下属问题。
- 周二：只做“规则管理”，产出至少 1 条自动化质检规则。
- 周三：只做“系统推演”，完成 1 份影响域矩阵或状态机图。
- 周四：只做“业务审判”，完成 1 份极端场景边界审查记录。
- 周五：只做“职场算法”，完成 1 条审计日志 + 1 条踩坑复盘。

## 常见问题

### Q1: 没看到消息推送？

A: 当前是 `--no-deliver`，不会推送到外部渠道。请用 `openclaw cron runs --id <job_id> --limit 20` 查看结果。

### Q2: 想发到 Telegram/Slack？

A: 先配置频道，再在 cron 中追加 `--announce --channel <channel> --to <target>`。

### Q3: 输出太空泛？

A: 在 Prompt 里增加硬约束（时长、产出模板、禁止空话、每项必须给完成标准）。
