# 操作手册：每日训练任务清单（OpenClaw 新手版）

## 1. 目标

把你的 5 个修炼主题做成一个工作日自动运行的任务：

- 每周一到周五自动生成「当日训练任务清单」
- 每条任务都可执行、可验收、有产出物
- 支持手动补跑、查看历史、随时调整规则

---

## 2. 前置条件

在本机 macOS 终端确认以下 3 件事：

```bash
openclaw --version
openclaw gateway health --json
openclaw cron status
```

通过标准：

- `openclaw --version` 有版本号输出
- `gateway health` 返回 `"ok": true`
- `cron status` 显示 scheduler 已启用（`enabled: true`）

---

## 3. 一次性创建任务

### 3.1 写入 Prompt（直接复制）

```bash
PROMPT=$(cat <<'EOT'
你是“资深量化/业务开发成长教练”。请生成“今天的训练任务清单”。

用户画像：
- 8年资深量化/业务开发
- 目标：提升业务判断力、系统思维、团队杠杆和职场防御能力

周内主题映射（必须按当天星期自动选择）：
- 周一 -> 团队杠杆与异步决策
- 周二 -> 用契约与规则替代人肉管理
- 周三 -> 系统依赖与蝴蝶效应推演
- 周四 -> 业务直觉与逻辑边界
- 周五 -> 职场算法与逆向工程

5个主题细化动作库（当天仅选对应主题，但要从库中抽取可执行动作）：

主题一：业务直觉与逻辑边界
- 翻译缝隙审查：把因子自然语言转 if/else 伪代码，找模糊边界
- 极端物理场景代入：涨停/跌停/停牌/一字板/新股首日是否符合常识
- 赌工资终审：不敢真金白银跟信号就退回重定义边界

主题二：系统依赖与蝴蝶效应推演
- 画影响域矩阵：上游/下游/缓存清洗影响
- 生命周期状态机推演：草稿->测试->上线->废弃，重点边缘状态
- 异常熔断设计：超时10秒或空列表时系统是否降级与告警

主题三：用契约与规则替代人肉管理
- Schema绝对独裁：完善输入输出规范
- 防伪表格：需求改成带边界与物理含义的填空题
- 代码质检关卡：自动化脚本拦截不合格交付

主题四：团队杠杆与异步决策
- 忍住不敲键盘：让老员工给两套方案对比
- 拦截越级提问：新人先走mentor链路
- 批注RFC文档：只提漏洞，不直接给答案

主题五：职场算法与逆向工程
- XY问题剥离术：追问真实目标Y而非表象X
- 强制生成审计日志：口头结论转文字确认
- 睡前5分钟异常复盘：踩坑实录/人际预警

输出格式（严格遵守）：
1) 今日主题（1行）
2) 今日目标（2-3行，可量化）
3) 25分钟深度训练任务（3项，每项含：动作、产出物、完成标准）
4) 10分钟风险防御任务（2项，每项含：触发条件、应对动作）
5) 今日沟通话术（2段，可直接复用）
6) 晚间复盘模板（5个问题）
7) 明日衔接点（1-2行）

质量要求：
- 所有任务都必须可在“今天”完成，不能抽象
- 每个任务都要有可检查的产出（文档、脚本、评论、消息记录）
- 全中文输出，简洁但具体
EOT
)
```

### 3.2 创建 Cron Job（工作日 07:30，上海时区）

```bash
openclaw cron add \
  --name "daily-quant-routine" \
  --description "工作日每日训练任务清单" \
  --cron "30 7 * * 1-5" \
  --tz "Asia/Shanghai" \
  --session isolated \
  --no-deliver \
  --timeout-seconds 180 \
  --thinking medium \
  --message "$PROMPT"
```

说明：

- `--session isolated`：任务在独立会话执行，不污染主对话
- `--no-deliver`：先不推送到 Telegram/Slack，先在本地看结果
- `--thinking low`：稳定且省资源，适合日报类任务

---

## 4. 首次验收（必须当场做）

### 4.1 列出任务

```bash
openclaw cron list --all --json
```

你需要拿到 `id`（例如 `job_xxx`）。

### 4.2 立即手动跑一次

```bash
openclaw cron run <job_id>
```

### 4.3 查看运行历史

```bash
openclaw cron runs --id <job_id> --limit 20
```

### 4.4 查看任务正文输出

```bash
openclaw dashboard
```

验收标准（4 条都满足才算跑通）：

- 有 run 记录
- run 状态为成功
- 输出是中文
- 输出严格包含 1 到 7 的结构化块

---

## 5. 每天怎么用（10 分钟例行）

每天上班后按这个顺序执行：

1. 看今天自动生成的清单，先只做第 3 部分「25 分钟深度训练任务」
2. 每做完 1 项，立刻补产出物（文档、脚本、消息记录）
3. 下午处理第 4 部分「风险防御任务」
4. 下班前用第 6 部分模板复盘并填 5 个问题

建议你把产出物固定到一个目录，便于周五复盘：

- `daily-routine/YYYY-MM-DD/`

---

## 6. 常用维护命令

### 6.1 暂停/恢复任务

```bash
openclaw cron disable <job_id>
openclaw cron enable <job_id>
```

### 6.2 修改执行时间（例如改成 08:00）

```bash
openclaw cron edit <job_id> \
  --cron "0 8 * * 1-5" \
  --tz "Asia/Shanghai"
```

### 6.3 修改 Prompt（优化输出质量）

```bash
openclaw cron edit <job_id> \
  --message "这里替换成你的新 Prompt"
```

### 6.4 删除任务

```bash
openclaw cron rm <job_id>
```

---

## 7. 常见问题排查

### 7.1 任务创建了但不自动跑

先看调度器状态：

```bash
openclaw cron status
```

如果 disabled，先恢复网关调度器配置，再重启网关。

### 7.2 手动 run 有记录但看不到正文

检查是否用了 `--no-deliver`。这是正常行为。正文在本地会话里看：

```bash
openclaw dashboard
```

### 7.3 输出太空泛

直接改 Prompt，加硬约束：

- 每项必须给「产出物路径」
- 每项必须给「完成标准」
- 禁止“加强沟通/持续优化”这类空话

---

## 8. 升级为“可推送版”（可选）

当你本地跑稳定后，再开启外部推送：

```bash
openclaw cron edit <job_id> \
  --announce \
  --channel <channel> \
  --to <target>
```

---

## 9. 推荐执行节奏（与你主题库一致）

- 周一：团队杠杆与异步决策
- 周二：契约与规则管理
- 周三：系统依赖推演
- 周四：业务逻辑边界审判
- 周五：职场算法与复盘审计

执行原则：

- 一天只练一个主题
- 每天至少留下一个可审计产出
- 周五统一复盘本周全部产出

---

## 10. 实战练习计划（Day0-Day5）

这部分是给你直接上手练的。按顺序做，不要跳步骤。

### Day0：系统跑通（30-45 分钟）

目标：

- 你的 Cron 任务可创建、可运行、可查看结果。

操作：

```bash
openclaw gateway health --json
openclaw cron status
openclaw cron list --all --json
openclaw cron run <job_id>
openclaw cron runs --id <job_id> --limit 5
openclaw dashboard
```

产出物：

- 1 张截图：`cron list` 中看到目标 job。
- 1 张截图：`cron runs` 中看到最新 run。
- 1 份文本：今天生成的训练任务清单（复制到你的笔记）。

验收标准：

- run 状态成功。
- 输出是中文且包含 1-7 结构块。

### Day1：团队杠杆训练（周一主题）

目标：

- 当天至少完成 1 次“忍住不下场、只做分派和评审”。

操作：

- 从清单中选 1 条「25 分钟深度训练任务」执行。
- 找一个真实卡点，给下属/同事布置“两套方案对比”任务。
- 自己只做 RFC 批注，不直接写实现。

产出物：

- 1 条分派消息（聊天记录）。
- 1 份方案对比结论（5-10 行）。
- 1 条 RFC 批注记录。

验收标准：

- 你当天没有直接接管该任务编码。
- 两套方案都有明确 trade-off。

### Day2：规则管理训练（周二主题）

目标：

- 把“人肉把关”变成“规则把关”。

操作：

- 基于清单任务，补 1 条输入输出约束（Schema 或文档字段约束）。
- 增加 1 条自动校验逻辑（脚本/检查条件都可以）。
- 将明天任务 prompt 增加“必须给出产出路径+完成标准”硬约束。

可选命令（用于更新 cron 提示词）：

```bash
openclaw cron edit <job_id> \
  --message "这里替换成加强约束后的新 Prompt"
```

产出物：

- 1 条规则文档变更。
- 1 条自动化校验条件（脚本或伪代码）。

验收标准：

- 至少有 1 个“未达标即拒收”的硬规则。

### Day3：系统推演训练（周三主题）

目标：

- 学会在改动前做影响域推演，而不是事后救火。

操作：

- 选择 1 个正在改动的模块，画影响域矩阵（上游/下游/缓存）。
- 做 1 次异常演练：接口超时 10 秒或返回空列表，系统如何降级。

产出物：

- 1 张影响域矩阵图（或表格）。
- 1 份异常处理清单（触发条件、降级策略、告警动作）。

验收标准：

- 每个异常场景都有明确“系统行为”而非口号。

### Day4：业务审判训练（周四主题）

目标：

- 建立“违背业务物理常识就不放行”的习惯。

操作：

- 选 1 个因子/策略逻辑，写出 if/else 伪代码版本。
- 逐条过 5 个极端场景：涨停、跌停、停牌、一字板、新股首日。
- 做一次“赌工资终审”：写下不敢实盘跟随的原因。

产出物：

- 1 份伪代码草案。
- 1 份极端场景检查表。
- 1 条终审结论。

验收标准：

- 至少识别并修正 1 个边界漏洞。

### Day5：职场算法训练（周五主题）

目标：

- 减少口头模糊，建立可追溯的沟通审计链。

操作：

- 在 1 个需求沟通中使用 XY 追问（追目标，不追表象）。
- 对 1 次口头结论发“审计日志”确认消息。
- 睡前做 5 分钟《踩坑实录》。

产出物：

- 1 条 XY 追问对话记录。
- 1 条审计日志确认消息。
- 1 条踩坑复盘。

验收标准：

- 当天至少有 1 条“可追责”的书面确认。

### Day5 结束后的复盘动作（10 分钟）

操作：

1. 回看本周 5 天产出物。
2. 标记最有效的 2 个动作和最浪费的 2 个动作。
3. 调整下周 Prompt（只保留有效动作，删除空泛动作）。

建议命令：

```bash
openclaw cron edit <job_id> --message "更新后的下周 Prompt"
```
